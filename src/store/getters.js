export default {
    getAllUsers(state) {
        return state.allUsers
    },

    getCurrentUser(state) {
        return state.currentUser
    },

    getErrors(state) {
        return state.errors
    },

    getCountries(state) {
        return state.allCountries
    }
}